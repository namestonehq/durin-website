name: Build

on:
  push:
  pull_request:

jobs:
  build:
    name: Website
    runs-on: ubuntu-latest
    env:
      NEXT_PUBLIC_ALCHEMY_API_KEY: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        run: npm ci

      - name: Build
        run: npm run build
